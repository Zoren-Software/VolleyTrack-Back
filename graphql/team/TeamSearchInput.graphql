"TeamSearchType"
input TeamSearchInput {

    "String to search for in teams name"
    search: String

    "Filter by position ids"
    positionsIds: [Int]

    "Filter by players ids"
    playersIds: [Int]

    "Filter by users ids"
    usersIds: [Int]

    "Ignore ids selected"
    ignoreIds: [Int]
}
